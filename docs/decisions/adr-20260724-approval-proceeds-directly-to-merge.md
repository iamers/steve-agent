---
status: accepted
date: 2026-07-24
---

# Approval proceeds directly to merge

## Context

Not recorded when the decision was taken.

First recorded in the repository on 2026-07-24 (commit 4651d81). The decision may have
been taken earlier; the original date is not recorded.

## Decision

At the `approve` response the PR is merged without a second approval round. A single positive signal is enough. (For `safe` PRs the phase 2 gate executes the merge; for `propagation` and `blast` PRs the merge remains a human action on GitHub. See the status table.)

## Consequences

Not recorded when the decision was taken.

## Alternatives considered

Not recorded when the decision was taken.
